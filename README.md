# Module 3 – Interactive Calculator (CLI)

A command-line calculator built with Python using OOP design, pytest testing, and 100% coverage.

---

## Features
- REPL interface
- add / subtract / multiply / divide
- input validation
- graceful error handling
- unit tests + parameterized tests
- 100% test coverage
- GitHub ready project structure

---

## Setup

Create virtual environment:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
